RewriteEngine On
RewriteBase /

# Serve existing files and directories as static assets
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Front controller
RewriteRule ^(.*)$ index.php [QSA,L]
